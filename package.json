{
  "name": "trade-axis",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "preinstall": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}